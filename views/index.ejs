<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>home page</title>
</head>
<body>
    <h1>Hello World</h1>
    <% data.forEach(val =>{ %>
        <div>
            <img src="<%= val.image %>">
            <h2><%= val.title %></h2>
            <ul>
                <%val.authors.forEach(value =>{%>
                <li><%= value %></li>
                <%});%>
            </ul>
            <p><%= val.description %></p>
            <button><a href="/details/<%=val.id%>"> detials</a></button>
        </div>
        <% });%>
</body>
</html>